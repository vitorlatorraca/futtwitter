/**
 * Re-exports the canonical position → PT-BR abbreviation function.
 * Kept for backward compatibility — all imports resolve to the shared module.
 */
export { positionToPtBr as toPtBrAbbrev } from '@shared/positions';
export type { CanonicalPosition } from '@shared/positions';
